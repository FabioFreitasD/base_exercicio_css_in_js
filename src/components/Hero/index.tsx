import { DivFormulario, Formulario, HeroTitle } from './styles'

const Hero = () => (
  <Formulario>
    <DivFormulario>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </DivFormulario>
  </Formulario>
)

export default Hero
